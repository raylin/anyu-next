# Staging + Production Env Mirror Reconciliation v1

Date: 2026-06-06

## Summary

Reconciled the ignored server env mirrors against Vercel Preview(staging) and Vercel Production key-name inventories without printing values or committing env files.

Local mirrors remain:

- `apps/web/.env.staging` for Vercel Preview(staging)
- `apps/web/.env.production` for Vercel Production

Admin CLI remains outside this model: `pnpm ops` reads `ADMIN_API_TOKEN` only from explicit shell/process env and does not load app env mirrors.

## Production Freeze

Production remained frozen/fail-closed during validation:

- Public `/`, `/refund`, `/legal`: reachable via production preflight.
- Checkout route: fail-closed.
- Fake-paid/operator routes: fail-closed.
- Production runtime was not enabled.
- No payment, Email, or LINE message was run.

## Vercel Key Inventory

### Preview(staging)

Vercel Preview(staging) key names were inspected without values.

Result:

- Host key-name inventory was available.
- Several Preview(staging) values are branch-scoped as `Preview (staging)`.
- Sensitive/protected values were not readable through the safe pull path.

### Production

Vercel Production key names were inspected without values.

Result:

- Host key-name inventory was available.
- Sensitive/protected values were not readable through the safe pull path.
- Host value shape remains `host_value_shape_unverified` by design.

## Local Mirror Reconciliation

### `.env.staging`

Actions:

- Reorganized into consistent commented sections.
- Preserved existing non-empty local values.
- Synced visible Vercel value names into the local mirror where Vercel exposed them.
- Generated internal auth values where allowed.
- Left protected/provider/stateful keys blank under owner-fill when Codex must not infer or rotate them.

Generated by Codex and synced to Vercel Preview(staging):

- `CRON_SECRET`
- `INTERNAL_JOB_SECRET`

Visible Vercel values synced into local mirror:

- `ENABLE_OPERATOR_FAKE_PAID_SUCCESS`
- `NEXT_PUBLIC_LINE_ADD_URL`

Owner-fill-required staging keys:

- `ANALYSIS_CACHE_HASH_SECRET`
- `ANTHROPIC_FALLBACK_MODEL`
- `ANTHROPIC_FAST_MODEL`
- `ENABLE_NEWEBPAY_CHECKOUT`
- `ENABLE_OPERATOR_EMAIL_RECOVERY_SMOKE`
- `ENABLE_OPERATOR_LINE_RECOVERY_SMOKE`
- `ENABLE_OPERATOR_RECOVERY_LINK_SMOKE`
- `ENABLE_PAID_GENERATION_JOBS`
- `ENABLE_PAID_GENERATION_PROCESSOR`
- `ENABLE_PAID_JOB_QUEUE_TRIGGER`
- `LINE_CHANNEL_ACCESS_TOKEN`
- `LINE_CHANNEL_SECRET`
- `LINE_RECOVERY_MESSAGE_PROVIDER`
- `LINE_RECOVERY_RECIPIENT_ENCRYPTION_KEY`
- `MODEL_STRATEGY`
- `NEWEBPAY_CHECKOUT_URL`
- `NEWEBPAY_ENVIRONMENT`
- `NEWEBPAY_NOTIFY_URL`
- `NEXT_PUBLIC_LINE_LIFF_ID`
- `NEXT_PUBLIC_LINE_LIFF_URL`
- `PAID_ACCESS_TOKEN_HASH_SECRET`
- `PAID_JOB_QUEUE_PROVIDER`
- `PAID_JOB_QUEUE_TOPIC`
- `PAYMENT_CHECKOUT_SESSION_SECRET`
- `PAYMENT_RECOVERY_CONTACT_ENCRYPTION_KEY`
- `PAYMENT_RECOVERY_CONTACT_HASH_SECRET`
- `PAYMENT_RECOVERY_LINK_TOKEN_SECRET`

Notes:

- Preview(staging) generated keys were synced to the branch-scoped `preview staging` target.
- A plain Preview sync attempt failed due target scope; branch-scoped sync succeeded.
- No real staging Email/LINE channel validation was run.

### `.env.production`

Actions:

- Reorganized into consistent commented sections.
- Preserved existing non-empty local values.
- Generated internal auth values where allowed.
- Set local production queue/processor gates to disabled values for dry-run/fail-closed validation.
- Left provider/stateful protected keys blank under owner-fill when Codex must not infer or rotate them.

Generated by Codex and synced to Vercel Production:

- `CRON_SECRET`
- `INTERNAL_JOB_SECRET`

Visible Vercel values synced into local mirror:

- `ANALYSIS_GLOBAL_DAILY_LIMIT`
- `ANALYSIS_IP_HOURLY_LIMIT`
- `ANALYSIS_SESSION_DAILY_LIMIT`
- `NEXT_PUBLIC_LINE_ADD_URL`

Local intended disabled gate values set:

- `ENABLE_PAID_GENERATION_PROCESSOR`
- `ENABLE_PAID_JOB_QUEUE_TRIGGER`

Owner-fill-required production keys:

- `ANALYSIS_CACHE_HASH_SECRET`
- `LINE_CHANNEL_ACCESS_TOKEN`
- `LINE_CHANNEL_SECRET`
- `LINE_RECOVERY_MESSAGE_PROVIDER`
- `LINE_RECOVERY_RECIPIENT_ENCRYPTION_KEY`
- `NEWEBPAY_CHECKOUT_URL`
- `NEWEBPAY_ENVIRONMENT`
- `NEWEBPAY_NOTIFY_URL`
- `PAID_ACCESS_TOKEN_HASH_SECRET`
- `PAID_JOB_QUEUE_PROVIDER`
- `PAID_JOB_QUEUE_TOPIC`
- `PAYMENT_CHECKOUT_SESSION_SECRET`
- `PAYMENT_RECOVERY_CONTACT_ENCRYPTION_KEY`
- `PAYMENT_RECOVERY_CONTACT_HASH_SECRET`
- `PAYMENT_RECOVERY_LINK_TOKEN_SECRET`

Production preflight blocker subset:

- `PAYMENT_CHECKOUT_SESSION_SECRET`
- `PAID_ACCESS_TOKEN_HASH_SECRET`
- `PAYMENT_RECOVERY_LINK_TOKEN_SECRET`
- `PAYMENT_RECOVERY_CONTACT_HASH_SECRET`
- `PAYMENT_RECOVERY_CONTACT_ENCRYPTION_KEY`
- `LINE_RECOVERY_RECIPIENT_ENCRYPTION_KEY`
- `LINE_CHANNEL_ACCESS_TOKEN`

Notes:

- Production Vercel env was changed only for generated internal auth: `CRON_SECRET`, `INTERNAL_JOB_SECRET`.
- Production was not redeployed.
- Runtime/checkout remained disabled.

## Env Governance Checks

- `apps/web/.env.staging` is ignored by git.
- `apps/web/.env.production` is ignored by git.
- `apps/web/.env.local` is not active as a server mirror.
- `apps/web/.env` is not active as a server mirror.
- Admin CLI remains pure Admin API client and does not load app env mirror files.

## Validation

Commands run:

- `cd apps/web && corepack pnpm run qa:module01:local`
- `cd apps/web && corepack pnpm run qa:module01:staging`
- `cd apps/web && corepack pnpm run qa:module01:production-preflight`

Results:

- `qa:module01:local`: pass
- `qa:module01:staging`: pass
- `qa:module01:production-preflight`: blocked

Production preflight category:

- `blocked_empty_local_mirror_secret`

Production safety inside preflight:

- pass / fail-closed

## Blockers

Production controlled smoke remains blocked until owner fills the production local mirror for the listed owner-fill-required critical keys and the hardened preflight returns `pass_ready_for_controlled_smoke`.

Do not resume production runtime enablement until this is resolved.

## Theme Route Preservation

Theme Architecture remains preserved and not implemented in this task:

- Hybrid Theme Park Model adopted.
- Module 01 Riso-only adopted.
- Core Shell neutral editorial adopted.
- Shared Flow Templates adopted.
- Module Theme Architecture Implementation Plan v0 remains a future design implementation track.

## Tech Debt / Cleanup Review

New technical debt introduced:

- None in tracked code.

Existing technical debt observed:

- Protected production stateful/provider values still require owner-side local mirror restoration.
- Preview(staging) branch-scoped Vercel env requires explicit `preview staging` targeting for future syncs.

Opportunistic cleanup completed:

- Ignored env mirrors were sectioned and commented consistently.

Deferred cleanup candidates:

- Add a safe documented helper for branch-scoped Vercel Preview env sync, if this operation recurs.
- Consider a runtime config-shape endpoint only if owner approves the Admin-authenticated read-only surface.

## Recommended Next Step

Owner fills the listed `.env.production` owner-fill-required critical values, then rerun:

```bash
cd apps/web && corepack pnpm run qa:module01:production-preflight
```

If it passes, the next task can be Controlled Production Payment Smoke v1 Clean Retry.
