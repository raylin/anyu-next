# Design System

Canonical design system docs live here.

- `docs/design-system/anyu-design-system-v1.md` is the canonical markdown reference.
- `docs/design-system/tokens.css` is the canonical token file.
- `apps/web/src/styles/tokens.css` is the app import copy.
- Token changes should be made in `docs/design-system/` first, then synced into app styles.
